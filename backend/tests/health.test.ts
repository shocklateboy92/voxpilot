import { describe, expect, it } from "bun:test";

// TODO: Phase 3 — re-enable once health endpoint is added back
//       (or replace with OpenCode server health check).

describe("health", () => {
  it.skip("stubbed — pending Phase 3 reimplementation", () => {
    expect(true).toBe(true);
  });
});
