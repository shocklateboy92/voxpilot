import { describe, expect, it } from "bun:test";

// TODO: Phase 3 — re-enable once artifact service is reimplemented
//       against the OpenCode session store.

describe("artifacts CRUD", () => {
  it.skip("stubbed — pending Phase 3 reimplementation", () => {
    expect(true).toBe(true);
  });
});
